<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bartomiej.pianistbooster">

    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        android:permissionGroup="android.permission-group.MICROPHONE"
        android:protectionLevel="dangerous" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ModeChooseActivity" />
        <activity android:name=".game_modes.freestyle_mode.FreestyleActivity" />
        <activity android:name=".game_modes.learn_notes_mode.LearnNotesActivity" />
        <activity android:name=".game_modes.learn_notes_mode.FinishActivity" />
        <activity android:name=".statistics.HallOfFameActivity" />
        <activity android:name=".tests.TestingActivity"></activity>
    </application>

</manifest>